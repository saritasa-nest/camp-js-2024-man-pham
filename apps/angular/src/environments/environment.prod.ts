/**
 * Application environment configuration for production.
 *
 * Defines the environment configuration for the application,
 * specifying that the application is in production mode.
 *
 * @constant
 * @property {boolean} production Indicates if the application is in production mode.
 */
export const environment = {
	production: true,
	apiUrl: 'https://api.camp-js.saritasa.rocks/api/v1/',
	apiKey: 'cc1c5afc-babc-4f50-a579-283d3cd66bd2',
};
